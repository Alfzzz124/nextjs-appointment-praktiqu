# Dependencies
node_modules/
.pnp/
.pnp.js

# Build
.next/
out/
build/
dist/

# Testing
coverage/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Prisma
prisma/*.db
prisma/*.db-journal

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
Thumbs.db
# NTFS alternate data streams, created whenever a file is downloaded on Windows and
# opened through WSL. 379 of them arrived with the two vendored projects below.
*:Zone.Identifier
*:SmartScreen

# Agent worktrees — 100 MB of repo copies, and each carries its own .git.
.claude/worktrees/

# Logs
*.log
logs/

# MCP Config (contains secrets)
.mcp.json
# Added by code-review-graph
.code-review-graph/
.deploy-artifacts/

# Generated from docs/api/openapi.yaml by `npm run postman` — derived, not source.
docs/api/postman-collection.json

# Landing page demos — 347 MB of HTML with base64-embedded assets, plus two archives
# of the same content. The .zip (166 MB) and .rar (128 MB) are both over GitHub's
# 100 MB hard limit and would be rejected on push; the directory would bloat every
# clone permanently. Anchored to the repo root so the already-tracked archives
# elsewhere in the tree are unaffected. Kept on disk, out of git.
/landing-page/
/landing-page.zip
/landing-page.rar
